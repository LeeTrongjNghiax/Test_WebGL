const ZERO = 0.;
const ONE = 1.;
const NUM_OF_EDGE_IN_PENTAGON = 5;

const PHI = (ONE + NUM_OF_EDGE_IN_PENTAGON ** (ONE / 2)) / 2;
const RECIPROCAL_OF_PHI = ONE / PHI;
const ALPHA = 1.;

const WHITE       = [1., 1., 1.];
const GREEN       = [0., .5, 0.];
const RED         = [1., 0., 0.];
const BLUE        = [0., 0., 1.];
const YELLOW      = [1., 1., 0.];
const PURPLE      = [160 / 255,  32 / 255, 240 / 255];
const LIGHT_GREEN = [0., 1., 0.];
const ORANGE      = [1., .5, 0.];
const LIGHT_BLUE  = [.0, 1., 1.];
const BEIGE       = [245 / 255, 245 / 255, 220 / 255];
const PINK        = [255 / 255, 192 / 255, 203 / 255];
const GRAY        = [.5, .5, .5];